#!/usr/bin/make -f

#export DH_VERBOSE=1
export PYBUILD_NAME=python3-cyberfusion-database-support
export PYBUILD_DISABLE=test

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_installsystemd:
	dh_installsystemd --name=wait-wsrep-ready --no-start --no-enable
